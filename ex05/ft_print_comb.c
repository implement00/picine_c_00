#include <stdio.h> // standard input, ouput
#include <unistd.h> // uni for; UNIX standard 

void ft_print_comb(void) {

    /*
    nique combinations of three distinct digits
    WHERE; both the digits within each combination AND he combinations themselves in
    ascending order
    
    e.g; 
    12, 013, 014, 015, 016, 017, 018, 019, 02

    | What is mean is that the number <abc> shoud be like this c > b, b > a . 
    
    *987 is not included because 789 already covers that combination.
    THis is not includes because 789 makes c < b . Not allowed.
    */

    int a;
    int b;
    int c;

    printf("This one I can't seem to do... I get it, just can't get the logic.");
}

int main(void) {
    ft_print_comb();
}
